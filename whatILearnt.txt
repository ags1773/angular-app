local reference: <input #variableName>

'variableName' will hold a reference to the <div> element (including all its properties) and will be accessable throughout
the html template

the 'value' can be got by doing variableName.value. 'value' is available because it's an <input>
