Assignment

1) Add input field which updates a property "username" (use two-way binding)
2) Output the username in a para below the input (Use string interpolation)
3) Add a clear button which resets the username and can only be clicked if username is NOT empty
