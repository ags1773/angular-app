Directives - ngIf

ngIf is a built-in directive. It conditionally shows/hides an html element, based on IF condition

*ngIf: The '*' is extra info for Angular. It's added because ngIf a structural directive. It changes the strucrure
          of the DOM (i.e. it either adds or removes the DOM element)
