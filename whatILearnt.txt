*************************************
**    Services - AccountsService   **
*************************************

The app is now set to use AccountsService, which handles all the accounts data mamipulation, thus DRYing up the components.

However, the app is broken (even though most code is fine) because of incorrect hierarchical injection of the AccountsService
