Directives - assignment 3 part II

*ngFor="let cnt of clickArray; let i = index" -> the var i contains index. ('index' is a reserved keyword)
this is used because date object can't be used to keep count; unlike the cnt used previously
